VAJRA NYAY — BACKEND FIXED

ROOT FILES:
server.js
package.json

RENDER:
Build Command: npm install
Start Command: npm start

REQUIRED ENVIRONMENT VARIABLES:
GROQ_API_KEY = your existing Groq key
FRONTEND_ORIGIN = https://ramkaranbhambhu201-ship-it.github.io
EVIDENCE_ENCRYPTION_KEY = your existing 32-byte base64 key

Do not put API keys in the GitHub frontend.

The supplied server.js is preserved as the backend source.
It contains /api/health and /api/pocket-rights routes.
